flowchart TB
    subgraph app["Single application / process"]
        direction TB
        ui["Presentation<br/>(UI)"]
        logic["Business logic"]
        data["Data storage"]

        ui --> logic --> data
    end

    user["User"] --> app
